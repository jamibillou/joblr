require 'spec_helper'

describe Authentification::Linkedin do

  it 'should be tested' # do
    # We're going to need to build a stub for that, something that behaves like the Linkedin API but isn't.
    # Because we're obviously not going to do API calls in the test suite
  # end
end