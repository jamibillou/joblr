= link_to '', root_path, class: 'brand'
%ul.nav
  - if user_signed_in?
    %li= link_to t('profile'), current_user
  - else
    %li= link_to t('pages.overview.title'), "#{root_url(subdomain: 'www')}home"

    - if Rails.env.development?
      %li= link_to t('pages.style_tile.title'), style_tile_path