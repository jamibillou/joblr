.span12
  .row-fluid
    .span2
      .visible-phone= render 'show_buttons',  user: @user
    .span6
      = render 'show_text', text: @user.profile.text
      .well#profile
        = render 'show_identity',             user: @user
        = render 'show_experience_education', user: @user
        = render 'show_skills',               user: @user
        = render 'show_qualities',            user: @user
        = render 'show_more',                 user: @user
    .span2
    .span2
      .hidden-phone= render 'show_buttons',   user: @user
      = render 'email_sharing_modal'