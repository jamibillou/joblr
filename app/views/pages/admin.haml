.row-fluid.admin
  .span12
    .row-fluid
      .span4
        .hero-unit
          %h1= BetaInvite.count
          .big invites
          %br/
      .span4
        .hero-unit
          %h1= User.count
          .big users
          %br/
      .span4
        .hero-unit
          %h1= User.select{|user| user.social?}.count
          .big
            social
            %br/
            users
    .row-fluid
      .span4
        .hero-unit
          %h1= User.select{|user| signed_up?(user)}.count
          .big
            completed
            %br/
            profiles
      .span4
        .hero-unit
          %h1= SharingEmail.count
          .big email sharings
          %br/
      .span4
        .hero-unit
          %h1= User.select{|user| user.admin?}.count
          .big admins
          %br/
.row-fluid.admin
  = render 'users/index'
  = render 'beta_invites/index'
